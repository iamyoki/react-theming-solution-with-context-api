// eslint-disable-next-line no-unused-vars
import { Theme, SerializedStyles } from '@emotion/react'

/**
 * themeCSS
 * @param {(theme: Theme)=>SerializedStyles} callback
 */
function themeCSS(callback) {
  return callback
}

export { themeCSS }
